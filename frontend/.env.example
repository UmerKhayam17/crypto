# Backend API base URL (no trailing slash)
VITE_API_URL=http://localhost:5000

# WebSocket uses the same host as VITE_API_URL (http→ws) at /ws

# Shown on admin login hint (must match backend ADMIN_EMAIL)
VITE_ADMIN_EMAIL=admin@cryptohaven.com
