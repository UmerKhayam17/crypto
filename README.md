# Crypto Haven Net

This repository contains the full-stack Crypto Haven Net application.

## Structure

- `backend/` — API server, models, controllers, routes, and websocket services.
- `frontend/` — Vite + React + TypeScript client interface.

## Getting Started

Install dependencies for both apps and run them locally.
